{% extends 'base.html.twig' %} {% block title %}Tous les albums{% endblock %}
{% block body %}
<h1 class="text-center text-2xl font-extrabold my-5">Tous les albums</h1>

<div class="grid grid-cols-4 gap-4 px-6">
  {% for album in albums %}
    {% include './components/album-card.html.twig' %}
  {% else %}
  <span>Aucun album trouvé</span>
  {% endfor %}
</div>
<a href="{{ path('app_album_new') }}">Create new</a>
{% endblock %}
